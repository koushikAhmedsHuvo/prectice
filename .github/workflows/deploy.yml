name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code and redeploy
        run: |
          cd ~/prectice
          git pull origin main
          docker compose up -d --build
